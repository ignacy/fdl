#! /usr/bin/env ruby
$:.unshift File.join(File.dirname(__FILE__), *%w[.. lib])

require "rubygems"
require 'fdl'

path = File.expand_path(File.dirname(__FILE__)).gsub(/bin/, "config.ru")
%x[rackup #{path} -p "8181"]
